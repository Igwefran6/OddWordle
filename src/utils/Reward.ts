export function Reward(difficulty: number) {
  let creditReward;
  let scoreReward;
  if ((difficulty = 4)) {
    creditReward = Math.floor(difficulty * 2);
    scoreReward = difficulty * 10;
  }
  if ((difficulty = 5)) {
    creditReward = Math.floor(difficulty * 2);
    scoreReward = difficulty * 10;
  }
  if ((difficulty = 6)) {
    creditReward = Math.floor(difficulty * 2);
    scoreReward = difficulty * 10;
  }
  if ((difficulty = 7)) {
    creditReward = Math.floor(difficulty * 2);
    scoreReward = difficulty * 10;
  }
  if ((difficulty = 8)) {
    creditReward = Math.floor(difficulty * 2);
    scoreReward = difficulty * 10;
  }
  if ((difficulty = 9)) {
    creditReward = Math.floor(difficulty * 2);
    scoreReward = difficulty * 10;
  }
  if ((difficulty = 9)) {
    creditReward = Math.floor(difficulty * 2);
    scoreReward = difficulty * 10;
  }
}
